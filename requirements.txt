# Основная библиотека для Telegram ботов
pyTelegramBotAPI==4.14.0

# Для HTTP запросов к API валютных курсов
requests==2.31.0

# Для работы с переменными окружения (токены, ключи API)
python-dotenv==1.0.0

# Для логирования (записи действий бота в лог файлы)
loguru==0.7.2